{
  "rules": {
    ".read": false,
    ".write": false,

    "registros": {
      ".read": "auth != null && auth.token.role != null",
      ".indexOn": ["fecha"]
    },

    "cortes": {
      ".read": "auth != null && auth.token.role != null"
    }
  }
}
